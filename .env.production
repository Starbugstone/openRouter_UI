# Public production metadata only. Never put secrets in VITE_* variables.
# OAuth callbacks and API attribution still use the browser's actual origin.
VITE_PUBLIC_SITE_URL=https://aiplayground.starbugstone.com
