# Optional public URL override for another deployment.
# Production defaults are tracked in .env.production; development omits canonical/OG URLs.
# OAuth callbacks and HTTP-Referer always use the browser's actual origin.
VITE_PUBLIC_SITE_URL=
