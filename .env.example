# Optional canonical/OG URL for the final production deployment.
# Leave empty for localhost, previews, and until the final hostname is chosen.
# OAuth callbacks and HTTP-Referer always use the browser's actual origin.
VITE_PUBLIC_SITE_URL=
